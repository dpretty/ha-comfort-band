# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.eggs/

# Belt-and-braces — no Node code lives here, but if someone runs npm
# (e.g. accidentally checking out an older branch with the card), don't
# accidentally commit node_modules.
node_modules/
package-lock.json

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local-only banned-words patterns (additions stay private; the script reads them too)
.banned-words.local
